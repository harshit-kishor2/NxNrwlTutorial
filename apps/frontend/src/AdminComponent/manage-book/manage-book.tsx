import React from 'react';

import './manage-book.css';

/* eslint-disable-next-line */
export interface ManageBookProps {}

export const ManageBook = (props: ManageBookProps) => {
  return (
    <div>
      <h1>Welcome to ManageBook!</h1>
    </div>
  );
};

export default ManageBook;
