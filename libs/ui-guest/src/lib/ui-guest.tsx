import React from 'react';

import './ui-guest.css';

/* eslint-disable-next-line */
export interface UiGuestProps {}

export const UiGuest = (props: UiGuestProps) => {
  return (
    <div>
      <h1>Welcome to ui-guest!</h1>
    </div>
  );
};

export default UiGuest;
